# Core analytics — deterministic layer
numpy==2.5.2
pandas==3.0.5
scipy==1.18.1
statsmodels==0.15.0          # MSTL seasonal decomposition
scikit-learn==1.9.0          # ridge/lasso ranking, isotonic calibration
duckdb==1.5.5                # analytics + document store (no external service)

# Service
fastapi==0.141.1
uvicorn==0.41.0
pydantic==2.13.4
pyyaml==6.0.3
httpx==0.28.1                # external signal feeds (Open-Meteo)

# Dev
pytest==9.1.1
pytest-cov==8.0.0
ruff==0.15.5
